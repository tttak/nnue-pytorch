# Core runtime dependencies for Python 3.11 / CUDA 13.0.
#
# PyTorch is intentionally installed separately by install_windows_cuda130.ps1
# so its dedicated CUDA wheel index is not used to resolve these packages.
pytorch-lightning==2.6.5
numpy==2.4.6
tensorboard==2.20.0
bitsandbytes==0.48.2
python-chess==0.31.4
cupy-cuda13x==14.2.0
numba==0.67.0
matplotlib==3.11.1
